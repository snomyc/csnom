spring.application.name=csnom-consumer
server.port=8090
#dubbo
dubbo.scan.base-packages=com.snomyc
dubbo.application.id=csnom-consumer
dubbo.application.name=csnom-consumer
dubbo.consumer.check=false

# 线程池类型
# fixed 固定大小线程池，启动时建立线程，不关闭，一直持有。(缺省)
# cached 缓存线程池，空闲一分钟自动删除，需要时重建。
# limited 可伸缩线程池，但池中的线程数只会增长不会收缩。只增长不收缩的目的是为了避免收缩时突然来了大流量引起的性能问题。
dubbo.application.protocol.threadpool=cached
# 线程池大小(固定大小)
dubbo.application.protocol.threads=100

#环境变量
spring.profiles.active=@activatedProperties@
